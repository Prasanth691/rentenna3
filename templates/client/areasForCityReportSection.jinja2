<div class="complaint-section report-section">
    <div class="report-section-header">
        <h2>Neighborhoods</h2>
    </div>

    <div class="quick-stats content">
        {% for area in subsections.areas.areas %}
            <a href="{{ area.getUrl() }}" class="tag-link">
                {{ area.name }}
            </a>
        {% endfor %}
    </div>
</div>